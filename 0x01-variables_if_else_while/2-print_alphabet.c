#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main() 
{
    int n = 97;

    while (n <= 172)
    {
        putchar(n);
	n++;
    }
    putchar('\n');
    return 0;
}
